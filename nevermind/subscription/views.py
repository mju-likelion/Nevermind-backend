from django.shortcuts import render
from django.http import JsonResponse


def add(req):
  #
  return JsonResponse({})


def get(req):
  #
  return JsonResponse({})


def update(req):
  #
  return JsonResponse({})


def delete(req):
  #
  return JsonResponse({})
